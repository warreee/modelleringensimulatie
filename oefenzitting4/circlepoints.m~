function [ points ] = circlepoints( n )
% returns an array with n points in a circle

points = zeros(n,2);
for i = 1 : n
    radius = rand();
    angle = rand() * 2 * pi;
    [x,y] = pol2cart(radius,angle);
    points(i,:) = ;
    
end


end

