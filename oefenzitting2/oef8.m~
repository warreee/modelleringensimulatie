load('Agrigento.mat');

[Q,R,P] = qr(Z);
normOr = norm(Z);

for k = 10 : 10 : 200
    temp = Q(:,1:k)*R(1:k,:);
    rel = norm(Z - temp)/norm(Z);
    rel_error = [rel_error, rel];
    %figure;
    %imagesc(temp);
   % waitforbuttonpress;
end

semilogy(